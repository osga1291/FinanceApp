package Gandara.Cash.Dao;
/*
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;
import Gandara.Cash.models.cashFlow;

import java.util.Date;
import java.util.LinkedHashMap;
import java.util.List;
import java.util.Optional;

@Repository
public interface cashFlowDao extends JpaRepository<cashFlow,Long> {

    Optional<cashFlow> findByUserAndMonthAndYearAndDay(Long userId, int month, int year, int day) throws ResourceNotFoundException;
    LinkedHashMap<Integer, Double> findByUserAndMonthAndYear(Long userId, int month, int year);
    List<LinkedHashMap<Integer,Double>>findByUserAndYear(Long userId, int year) throws ResourceNotFoundException;



}

*/
